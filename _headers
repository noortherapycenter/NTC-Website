# Access control for the admin area lives in netlify/edge-functions/staff-auth.js
# (server-side PIN, read from the STAFF_PIN environment variable). The old
# Basic-Auth block that used to sit here was removed: it duplicated that gate
# and put a plaintext password in a public repo.
